import Raylib
define function main(): {
   InitWindow(800,600,"woah raylib in my own lang is crazy")
   ~SetTargetFPS(60)
   while(not bool(WindowShouldClose())) do: {
      BeginDrawing()
      ClearBackground(100,0,230,55)
      DrawFPS(10, 10)
      EndDrawing()
   }
   CloseWindow()
}
